# Backend-specific gitignore patterns
# These are in addition to the root .gitignore

# Ensure .env files are ignored (even if root .gitignore is missed)
.env
.env.local
.env.*
!.env.example

# Storage directories (contain user data)
storage/
*.db
*.sqlite
*.sqlite3
