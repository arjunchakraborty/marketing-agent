# Minimal dependencies for Vercel deployment (under ~500MB bundle limit).
# For full local features (ChromaDB, image models, etc.) use: pip install -r requirements-full.txt

fastapi
uvicorn[standard]
sqlmodel
sqlalchemy
pydantic-settings
psycopg[binary]
python-multipart
pandas
httpx
openai>=1.0.0
anthropic>=0.18.0
websockets
pymongo
